# Amazon Bedrock Chat API endpoint (deployed via AWS SAM)
# Format: https://<api-id>.execute-api.<region>.amazonaws.com/chat/advisor
# Leave blank to disable live Bedrock calls (chat will show connection error gracefully)
VITE_CHAT_API_URL=
