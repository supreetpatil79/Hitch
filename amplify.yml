version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18 || nvm use 20 || true
        - cd frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
